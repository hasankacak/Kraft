if the variable in the parent class is defined
with same name same signature in the child class
There is two referans situatin
one of them from child class
one of them from parent class

if we are referancing from child class
        variable in the child class